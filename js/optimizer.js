/* optimizer.js
 * Calcula el orden de visita eficiente a partir de la matriz de
 * tiempos/distancias. Fuerza bruta para pocos pedidos; heurística
 * para muchos. Implementación pendiente (Fase 6b).
 */

export function calcularOrden({ ubicacionInicial, pedidos, matriz }) {
  const _ = { ubicacionInicial, pedidos, matriz };
  // Pendiente (Fase 6b).
  return null;
}